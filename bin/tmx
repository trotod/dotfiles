#!/bin/sh
#
# Creates or attaches session based on given name, or the directory name if not given

if [[ -z "$1" ]] ; then
  tmux new -As "$(basename "$PWD" | tr . - | sed 's/^-//')"
else
  tmux new -As "$1"
fi
