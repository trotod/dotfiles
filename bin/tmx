#!/bin/sh

# Creates or attaches session based on given name, or the directory name if
# not given.

if [ -z "$1" ] ; then
  tmux new -A -c "$PWD" -s "$(basename "$PWD" | tr '.' '-' | sed 's/^-//')"
else
  tmux new -A -c "$1" -s "$1"
fi
