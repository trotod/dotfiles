#!/bin/sh
#
# Creates a terminal window based on drawn size.
# Requires: urxvt, slop, wmutils

slopoutput="$(slop -t 0 -f '%w %h %x %y')" || exit 1

read -r w h x y << EOF
$slopoutput
EOF

urxvtc
sleep .01 # hack
wtp "$x" "$y" "$w" "$h" "$(pfw)"
