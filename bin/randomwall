#!/bin/sh
#
# picks a random wallpaper
# TODO ignore tiling ones with a better syntax

wallpapers_dir="$HOME/Pictures/wallpapers"
ignore_flags="-I hex.png -I line.png -I escheresque.png"

cd $wallpapers_dir
hsetroot -fill $(ls -1 $ignore_flags | shuf | head -1)
cd - > /dev/null
